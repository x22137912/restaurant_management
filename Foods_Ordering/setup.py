from setuptools import setup

setup(
    name='deliverycharges',
    version='0.1',
    description='A library for applying a deivery charges based on address selected',
    packages=['deliverycharges'],
    install_requires=[],
)